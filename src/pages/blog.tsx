import React from "react";
import Layout from "../components/Layout";
import type { PageProps } from "gatsby";

const BlogPage = () => {
  return <Layout location="blog">blog</Layout>;
};

export default BlogPage;
